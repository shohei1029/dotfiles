alias emacs='sl'
alias brewup='brew upgrade && brew cleanup'

alias pbcopy='clip.exe'

path=($path $HOME/bin(N-/))
export PATH="$HOME/.local/bin:$PATH"

# Linuxbrew (Homebrew on Linux) の初期化は .zshrc 本体で
# クロスプラットフォームに行う (antidote より前に PATH へ載せる必要があるため)。